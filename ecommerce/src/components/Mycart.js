import React from 'react'
import { CartContext } from './CartContext'

const Mycart = () => {
    return (
        // const { cart, toPay, removeFromCart } = useContext(CartContext);
        <div>

        </div>
    )
}

export default Mycart